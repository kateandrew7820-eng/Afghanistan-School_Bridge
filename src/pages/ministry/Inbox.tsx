import { VerificationInbox } from '@/components/VerificationInbox';

/**
 * Ministry-wide verification inbox.
 */
export default function MinistryInbox() {
  return (
    <VerificationInbox
      title="صندوق تأیید وزارت"
      subtitle="تمام ارسال‌های سراسر کشور — مرور، فیلتر و تأیید"
      scope={{}}
    />
  );
}
